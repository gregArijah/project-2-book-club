
{{#each books as |book| }}
<div class="row mb-4">
  <div class="col-md-5">
    <h2>
      <a href="/book/{{book.id}}">{{book.name}}</a>
    </h2>
    <p>
      {{book.image}}
      {{book.id}}
      {{book.name}}
      {{book.description}}
    </p>
    <p></p>
  </div>
  <div class="col-md-7">
    
  </div>
</div>
{{/each}}
